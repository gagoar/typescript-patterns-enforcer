{
  "name": "ts-patterns-engine",
  "private": true,
  "version": "0.3.0",
  "description": "Self-contained mechanical gate for the ts-patterns plugin. Bundled at release time; never reads or writes the target repo's own ESLint/tsconfig.",
  "type": "commonjs",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "pretest": "npm run build",
    "test": "node --test test/run.test.mjs",
    "release": "npm ci && npm run typecheck && npm run build && npm test"
  },
  "dependencies": {
    "typescript": "6.0.3"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "8.70.0",
    "@typescript-eslint/parser": "8.70.0",
    "@types/node": "22.10.2",
    "esbuild": "0.28.2",
    "eslint": "10.10.0",
    "eslint-plugin-promise": "7.3.0"
  }
}
